export function runBackup() {
  console.log("[backup] PostgreSQL (Neon) handles backups automatically.");
}
export function startBackupScheduler() {
  console.log("[backup] Using Neon PostgreSQL — cloud backups are automatic.");
}
